#include <stdio.h>
#include "triangle.h"

/* For this program to work , you must add the "triangle.h" and 
     "triangle.c" files to the project (using add existing menu).
*/

int main()
{
	printf("%f\n", area(3,4,5));

	return 0;
}